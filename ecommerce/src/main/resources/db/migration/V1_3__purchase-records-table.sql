CREATE TABLE IF NOT EXISTS purchase_records(id INT NOT NULL AUTO_INCREMENT, sku VARCHAR(15) NOT NULL, name VARCHAR(30) NOT NULL, price DECIMAL(5,2) NOT NULL, purchased_at DATETIME NOT NULL, client_cpf VARCHAR(11) NOT NULL, PRIMARY KEY(Id), CONSTRAINT fk_client_cpf FOREIGN KEY(client_cpf) REFERENCES clients(cpf));